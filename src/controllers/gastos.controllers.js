import axios from "axios";
import fs from "fs";
import { v4 as uuidv4 } from "uuid";


/* put pago */
export async function agregarGasto() {
    try {
     
    } catch (error) {
      res.status(500).json({ error: "error al obtener un user de la api" });
    }
  }
  
/* get pago para front  */
export async function editGasto() {
    try {
     
    } catch (error) {
      res.status(500).json({ error: "error al obtener un user de la api" });
    }
  }
  
  /* modificar el pago desde el json */
  export async function updateGasto() {
    try {
      
    } catch (error) {
      res.status(500).json({ error: "error al obtener un user de la apii" });
    }
  }

  /* eliminar un pago especifico en el json (find?filter? pop?)  */
  export async function deleteGasto() {
    try {
      
    } catch (error) {
      res.status(500).json({ error: "error al obtener un user de la apii" });
    }
  }